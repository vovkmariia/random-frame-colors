# random-frame-colors
Substance 3D Designer plugin that offers an alternative to a built-in "Add frame" functionality. When used, newly created frames are automatically assigned a random color, instead of the default blue.
